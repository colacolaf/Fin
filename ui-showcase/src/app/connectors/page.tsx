import { ConnectorsPage } from "@/components/connectors/connectors-page"

export const metadata = { title: "Connectors — Finance OS" }

export default function ConnectorsRoute() {
  return <ConnectorsPage />
}
